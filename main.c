#include <stdio.h>
#include <stdlib.h>
#include "loop.h"

int main(void) {
    // Load Config File

    // Run command Loop
    sh_loop();

    // perform any shutdown/cleanup
    return EXIT_SUCCESS;
}
